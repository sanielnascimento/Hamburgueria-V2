import styled from "styled-components";

export const Logo = styled.h1`
  font-size: 32px;
  font-weight: 700;
  line-height: 1;
  color: var(--grey6);
  span {
    font-size: 18px;
    font-weight: 900;
    line-height: 1;
    color: var(--secondary);
    margin-left: 6px;
  }
`;
